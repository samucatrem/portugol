programa {
  funcao inicio() {
    
    inteiro n1, n2, resultado

    escreva("Informe o primeiro valor: \n")
    leia(n1)

   limpa()
   escreva("informe o segundo valor: \n")
   leia(n2)

   limpa()
   //Executando a soma
   resultado = n1 + n2
   escreva("O resulatdo dos valores ", n1, " + ", n2, " = ", resultado)
   //Executando a subtração
   resultado = n1 - n2
   escreva("\nO resultado dos valores ", n1, " - ", n2, " = ", resultado)
   //Executando a multiplicação
   resultado = n1 * n2
   escreva("\nO resultado dos valores ", n1, " * ", n2, " = ", resultado)
   //Executando a multiplicação
   resultado = n1 / n2
   escreva("\nO resultado dos valores ", n1, " / ", n2, " = ", resultado)

  }
}
